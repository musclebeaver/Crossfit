spring:
  datasource:
    url: jdbc:mysql://localhost:3306/crossfit_db?serverTimezone=Asia/Seoul&useSSL=false
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379

app:
  auth:
    verify-email: false
